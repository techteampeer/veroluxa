# Web3Forms access key for the contact form (https://web3forms.com).
# Create a free key registered to contact@veroluxa.com, then set this
# in the Vercel project env (and locally in a .env file for dev).
# Without it, the contact form falls back to opening the visitor's mail client.
VITE_WEB3FORMS_KEY=
